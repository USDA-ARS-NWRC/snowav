# DEFAULTS
[snowav_recipe]
trigger:        has_section = snowav

snowav:
        apply_defaults = True


[run_recipe]
trigger:        has_section = run

run:
        apply_defaults = True

[forecast_recipe]
trigger:        has_section = forecast

forecast:
        apply_defaults = True

[plots_recipe]
trigger:        has_section = plots

plots:
        apply_defaults = True

[database_recipe]
trigger:        has_section = database

database:
        apply_defaults = True

[validate_recipe]
trigger:        has_section = validate

validate:
        apply_defaults = True

[report_recipe]
trigger:        has_section = report

report:
        apply_defaults = True

[query_recipe]
trigger:        has_section = query

query:
        apply_defaults = True

[inflow_recipe]
trigger:        has_section = inflow

inflow:
        apply_defaults = True

[diagnostics_recipe]
trigger:        has_section = diagnostics

diagnostics:
        apply_defaults = True
